# 陪伴型宠物情绪引擎设计

## 1. 情绪系统目标

这套引擎的目标不是把小窗做成"状态灯"，而是做成一个会陪着用户、但仍然尽量反映真实工作状态的宠物式存在。它需要同时满足四件事：

- 真实：真的在想、在做、在出问题时，要让情绪有迹可循。
- 自然：空闲时不能总是开心，要像有节律的小宠物一样会待机、会犯困、会偶尔调皮。
- 稳定：不能因为一条消息或一次工具调用就来回闪烁。
- 兼容：继续输出现有前端可直接消费的字段，不大改视觉层。

## 2. 核心原则

### 2.1 真实状态优先，但不把界面做死
- 正在工具调用、连续处理步骤、刚收到待回复消息时，优先体现真实行为。
- 真实行为结束后，不长期锁死在 `执行中`，而是自然过渡到 `思考中`、`待机中`、`调皮中` 或 `困困的`。

### 2.2 不只看最后一条，而看最近一段趋势
- 最近 6 到 8 条有效消息做加权分析，越新的权重越高。
- 不依赖单个词触发，而是把正向、疲惫、异常、惊讶、收尾等线索累计成趋势。

### 2.3 待机要像宠物，不像"默认开心"
- 无明确任务时，默认竞争关系是 `待机中 / 调皮中 / 困困的`。
- `开心中` 只在轻松、收尾、正向互动明显时出现，不再作为默认空闲态。

### 2.4 手动注入永远最高优先级
- `data/情绪注入.json` 一旦存在且未过期，直接覆盖自动判断。
- 这样可以保留人工导演感，不会被自动策略抢走控制权。

### 2.5 用分层融合代替散落 if-else
- 各层只负责提供"线索"和"分数"。
- 最终由融合层统一选情绪，再由防抖层控制切换节奏。

## 3. 状态分层架构

### 3.1 手动覆盖层
输入：`data/情绪注入.json`

职责：
- 判断注入是否存在、是否合法、是否过期。
- 若有效，直接输出最终状态。

优先级：最高。

### 3.2 真实行为层
输入：最近会话消息、工具调用痕迹、用户待回复状态。

职责：
- 判断是否仍在处理工具链。
- 判断是否刚收到用户消息、正在组织回复。
- 判断是否出现明显异常/失败。

典型输出：
- 工具活跃时强推 `执行中`
- 用户消息待回复时强推 `思考中`
- 多次错误/报错时抬升 `需注意`

### 3.3 会话氛围层
输入：最近数条消息文本的关键词趋势。

职责：
- 把最近会话抽象成几个连续维度：
  - 正向轻松度
  - 疲惫度
  - 异常压力
  - 惊讶度
  - 收尾感
- 不直接决定最终情绪，只给候选情绪加权。

### 3.4 时间 / 节律层
输入：当前本地时间、最近是否空闲。

职责：
- 深夜时提升 `困困的`
- 空闲较久时提升 `待机中`
- 白天轻松时给 `调皮中` 少量加成

### 3.5 执行尾声过渡层
输入：最近执行活跃时间、最近工具结束时间。

职责：
- 防止工具刚结束时长期卡在 `执行中`
- 让 `执行中 -> 思考中 -> 待机中/调皮中/困困的` 形成自然过渡

### 3.6 最终融合层
输入：各层对八种情绪的分数。

职责：
- 汇总分数，选出最高分情绪。
- 结合线索生成口吻自然的中文文案。
- 记录来源，如 `手动注入`、`真实行为`、`自动判断`、`异常保护`。

### 3.7 防抖层
输入：候选情绪、当前稳定情绪、停留时长。

职责：
- 控制最短停留秒数。
- 只有"候选明显更强"或"高优先级来源"时，才允许提前切换。
- 保证状态变化有呼吸感，不来回跳。

## 4. 情绪候选集合与触发因素

### `待机中`
用于中性空闲陪伴。

主要触发：
- 没有待处理消息
- 没有工具活跃
- 空闲时长变长
- 收尾后回到平稳状态

### `思考中`
用于收到需求、刚结束执行、正在组织表达。

主要触发：
- 最新仍有用户消息待回复
- 任务型关键词多
- 工具刚结束，正在整理结果

### `执行中`
用于真实处理工具或连续动作期间。

主要触发：
- 最近消息含 `toolCall` / `toolResult`
- 工具调用次数大于结果次数
- 连续多步执行还没完全落稳

### `开心中`
用于轻松顺畅且收尾感明显的时段。

主要触发：
- 正向关键词明显
- 最近刚回复完且气氛轻松
- "谢谢 / 好了 / 完成" 等收尾感存在

说明：不再作为默认空闲态。

### `调皮中`
用于轻松、活跃、带点小机灵的待机态。

主要触发：
- 正向关键词较多
- 没有强任务压力
- 白天或刚收尾不久
- 空闲但氛围不沉闷

### `困困的`
用于深夜、疲惫、长时间空闲或"明天再说"氛围。

主要触发：
- 当前时段在凌晨
- 最近消息出现困、睡、晚安、休息等词
- 空闲时间很长

### `惊讶中`
用于新奇、意外、被点醒的瞬间。

主要触发：
- 最近消息里出现"哇、居然、原来、没想到"等惊讶词
- 且没有明显异常压力覆盖

### `需注意`
用于真实波动、报错、失败、重试等状态。

主要触发：
- 最近几条消息累计出现错误/失败/异常/超时等词
- 比单条触发更强调趋势持续性

## 5. 趋势判断算法

## 5.1 输入窗口
- 从会话文件取最近 48 行，提炼出最近 18 条有效消息。
- 再从中重点分析最近 8 条，用于趋势加权。

## 5.2 有效消息过滤
过滤掉：
- 空文本
- 纯工具结果角色
- 本地状态检查、`curl /api/status`、`pkill` 等噪音内容

## 5.3 权重机制
- 最近 1 条消息权重最高
- 越往前权重越低，但不会归零
- 当前实现采用递减权重，大致从 `1.8` 逐步降到 `0.6`

## 5.4 趋势维度
对最近消息累计以下分值：
- 正向分
- 疲惫分
- 异常分
- 惊讶分
- 思考分
- 收尾分

每个维度由关键词命中次数乘以消息权重得到。

## 5.5 为什么这比"看最后一条"更好
因为用户最后一句可能只是"好"或"谢谢"，但前面几条可能刚经历：
- 连续工具调用
- 失败重试
- 深夜收尾
- 轻松聊天

只有看趋势，才能知道它应该落在：
- `待机中`
- `调皮中`
- `困困的`
- `开心中`
而不是被最后一句话误导。

## 6. 执行尾声过渡策略

执行尾声采用两段式过渡：

### 6.1 工具刚结束的 0~16 秒
- 如果前面刚有真实执行活跃，先提高 `思考中`
- 这表示"刚跑完动作，在整理结果"

### 6.2 工具结束后的 16~60 秒
- 降低 `执行中`
- 提高 `待机中` 与 `调皮中`
- 若深夜或疲惫氛围明显，再把一部分权重让给 `困困的`

结果上会形成：
- 正在处理：`执行中`
- 刚处理完：`思考中`
- 过渡稳定后：`待机中 / 调皮中 / 困困的`

## 7. 场景策略

### 7.1 深夜
- 凌晨 `0~6` 点给 `困困的` 明显加权
- 若同时长期空闲，则优先在 `困困的` 与 `待机中` 间切换

### 7.2 空闲
- 无待回复用户、无工具活跃时，空闲时间越长，`待机中` 越高
- 空闲很久且在深夜，再提高 `困困的`

### 7.3 轻松对话
- 正向分高、收尾分高、没有任务压力时，优先在 `调皮中` 和 `开心中` 中选择
- `调皮中` 更适合轻盈待机，`开心中` 更适合收尾满意

### 7.4 连续工具调用
- 工具调用次数多、且近期仍有工具痕迹时，稳定落在 `执行中`
- 连续执行刚结束后，不会立刻跳到开心，而是先过 `思考中`

## 8. 优先级与防抖策略

优先级顺序：
1. 手动注入
2. 异常保护
3. 真实行为层
4. 其他自动判断结果

防抖规则：
- 每种情绪有最短停留时间
- 若候选情绪与当前一致，仅更新文案，不强制切换
- 若候选分数比当前情绪高出明显阈值（当前实现约 `3.2`），允许提前切换
- `执行中` 刚结束后的短窗口内，不直接跳到闲置态，先给过渡空间

## 9. 与现有前端接口的兼容方式

引擎不改前端数据结构，仍统一写入：

```json
{
  "情绪": "调皮中",
  "标题": "调皮中",
  "文案": "气氛有点轻松呀，我在旁边晃一晃，等你继续点我。",
  "更新时间": "2026-03-07 02:00:00",
  "轮播序号": 123,
  "来源": "自动判断"
}
```

兼容方式：
- `data/状态.json` 作为中文主状态源
- `data/status.json` 同步写出英文字段兼容文件
- `server.py` 继续从 `data/状态.json` 提供 `/api/status`
- `web/app.js` 和 `web/styles.css` 无需改动即可兼容

## 10. 落地模块划分

### `status_store.py`
负责：
- 状态文件路径
- 默认状态
- 中文/兼容状态双写
- 手动注入读写

### `pet_emotion_engine.py`
负责：
- 会话读取
- 消息清洗
- 趋势分析
- 分层打分
- 文案生成
- 防抖

### `live_status_sync.py`
负责：
- 定时轮询
- 调用情绪引擎
- 把最终稳定结果写入状态文件

## 11. 可扩展方向

后续可以继续扩展，但不影响当前架构：

- 增加"情绪记忆"文件，让最近几分钟的心情更连续
- 对不同联系人建立不同陪伴风格
- 把关键词规则替换成更细粒度的轻量分类器
- 给文案模板加上节律轮换，减少重复感
- 把 `来源` 扩展为调试信息，便于排查为什么当前是这个情绪
